; Opcodes like MOV, ADD, etc.
(opcode) @keyword

; Identifiers (variables, labels)
(identifier) @variable

; Numbers (e.g., 42)
(number) @number

; Strings (e.g., "hello")
(string) @string

; Boolean literals
(boolean) @boolean

; Null literal
(null) @constant.builtin

; Comments
(comment) @comment